<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.aacdemo">

    <!--网络权限-->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <application
            android:name="com.example.aac_library.base.BaseApp"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:networkSecurityConfig="@xml/network_security_config">
        <activity android:name=".demo.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".demo.ReallyActivity"/>
        <activity android:name=".qrcode.QrCodeActivity"/>
        <activity android:name=".demo.AnkoActivity"/>
        <activity android:name=".asyntask.AsynActivity"/>
        <activity android:name=".demo.design_pattern.observer_pattern.ObserverActivity"/>
        <activity android:name=".wanandroid.home.activity.HomeActivity"/>
        <activity android:name=".demo.ServiceActivity"/>
        <service android:name=".service.MyService"
        android:process=":remote"/>
    </application>

</manifest>